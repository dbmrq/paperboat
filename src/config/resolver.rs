//! Model string parsing utilities for Villalobos.
//!
//! This module is mostly deprecated with the tier-based model system.
//! Model resolution is now handled by `ModelTier::from_str` and
//! `ModelFallbackChain::from_str` in the `models` module.

// This module is kept for potential backward compatibility but is not
// currently used. All tests have been removed as the functionality is
// obsolete with the new tier-based system.
